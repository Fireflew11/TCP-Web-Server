#include "httpCommands.h"

// Helper function to parse query strings
//map<string, string> parseQueryString(const string& query) {
//    map<std::string, std::string> queryParams;
//    istringstream queryStream(query);
//    string pair;
//    while (getline(queryStream, pair, '&')) {
//        size_t pos = pair.find('=');
//        if (pos != std::string::npos) {
//            string key = pair.substr(0, pos);
//            string value = pair.substr(pos + 1);
//            queryParams[key] = value;
//        }
//    }
//    return queryParams;
//}



